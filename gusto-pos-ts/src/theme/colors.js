import { theme } from "@/theme/theme";

export const drawerItemColors = {
  selectedBackground: theme.palette.primary.main,
  nonSelectedBackground: "transparent",
  hoverBackground: theme.palette.primary.light,
  selectedText: theme.palette.primary.contrastText,
  nonSelectedText: theme.palette.primary.main,
};
